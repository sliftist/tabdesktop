<Window x:Class="TabDesktop.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True" AllowsTransparency="True" Background="Transparent" SizeToContent="Height" Width="640">
    <Border Background="#FF1E1E1E" BorderBrush="#FF3A3A3A" BorderThickness="1" CornerRadius="6" Padding="10">
        <StackPanel>
            <TextBox x:Name="QueryBox" FontSize="20" Padding="6,4" Background="#FF2A2A2A" Foreground="White" BorderBrush="#FF3A3A3A" CaretBrush="White" TextChanged="OnQueryChanged" PreviewKeyDown="OnQueryKeyDown" />
            <ItemsControl x:Name="Results" Margin="0,8,0,0">
                <ItemsControl.ItemTemplate>
                    <!-- Mirrors the strip's tab tile (folder badge, thumbnail, favicon, title, browser-tab bar) so results look exactly like the tabs they stand for. -->
                    <DataTemplate>
                        <Border x:Name="ItemRoot" Padding="6,2" Margin="0,1" BorderBrush="#FF101010" BorderThickness="1" Cursor="Hand" MouseLeftButtonUp="OnResultClick">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#FF2A2A2A" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Background" Value="#FF1E5E8A" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid Opacity="{Binding Entry.IdleOpacity}">
                                <DockPanel>
                                    <Grid DockPanel.Dock="Left" Width="14" Height="13" Margin="0,1,4,0" VerticalAlignment="Top" ToolTip="Showing the process's directory">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Entry.DirectoryTitle}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Path Data="M0,12 L0,2 Q0,1 1,1 L5,1 Q5.6,1 6,1.6 L7,3 L13,3 Q14,3 14,4 L14,12 Q14,13 13,13 L1,13 Q0,13 0,12 Z" Fill="#D99C2B" />
                                        <Path Data="M0,12 L0,5.5 Q0,4.5 1,4.5 L13,4.5 Q14,4.5 14,5.5 L14,12 Q14,13 13,13 L1,13 Q0,13 0,12 Z" Fill="#FFD35C" />
                                    </Grid>
                                    <Image DockPanel.Dock="Left" Source="{Binding Entry.VideoThumbnail}" Height="48" Stretch="Uniform" Margin="0,0,4,0" VerticalAlignment="Top" RenderOptions.BitmapScalingMode="HighQuality">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Entry.VideoThumbnail}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    <Image DockPanel.Dock="Left" Source="{Binding Entry.IconImage}" Width="16" Height="16" Margin="0,0,4,0" VerticalAlignment="Top">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Entry.IconImage}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    <TextBlock Text="{Binding Entry.DisplayTitle}" Foreground="White" FontSize="12" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="48" VerticalAlignment="Center" />
                                </DockPanel>
                                <Rectangle Width="3" Margin="-6,-2" HorizontalAlignment="Left" Fill="#CCFFFFFF" IsHitTestVisible="False" ToolTip="A browser tab of an expanded window, not a window of its own">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Entry.BrowserTab}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                </Rectangle>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
