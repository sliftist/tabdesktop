<Window x:Class="TabDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TabDesktop"
        mc:Ignorable="d"
        Title="TabDesktop" Height="700" Width="1100" ShowActivated="False">
    <Window.Resources>
        <local:FractionOfHeightConverter x:Key="SectionMaxHeight" Fraction="0.8" />
    </Window.Resources>
    <TabControl x:Name="MainTabs" SelectionChanged="OnMainTabChanged">
        <TabItem Header="Settings">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="16,12" MaxWidth="900" HorizontalAlignment="Left">
                    <TextBlock Margin="0,0,0,12"><Run x:Name="BuildVersionRun" FontWeight="Bold" /><Run x:Name="BuildTimeRun" Foreground="#66000000" /></TextBlock>
                    <CheckBox x:Name="RunOnStartupCheck" Content="Run on startup — start TabDesktop automatically when you log in" Checked="OnRunOnStartupChanged" Unchecked="OnRunOnStartupChanged" Margin="0,0,0,8" />
                    <CheckBox x:Name="AdvancedModeCheck" Content="Advanced mode — show the extra tab strip buttons (rebuild, extension, height); basic mode shows only the count, collapse, and home buttons" Checked="OnAdvancedModeChanged" Unchecked="OnAdvancedModeChanged" Margin="0,0,0,8" />
                    <CheckBox x:Name="ShowWhenFullscreenCheck" Content="Show tab strips even when a window is fullscreen — normally strips hide on any monitor a fullscreen window covers, so movies and games stay unobstructed" Checked="OnShowWhenFullscreenChanged" Unchecked="OnShowWhenFullscreenChanged" Margin="0,0,0,8" />
                    <CheckBox x:Name="SearchEnabledCheck" Content="Enable search — a global hotkey opens a search box over all tabs; type, then Enter to jump to the selected tab" Checked="OnSearchEnabledChanged" Unchecked="OnSearchEnabledChanged" Margin="0,0,0,4" />
                    <StackPanel Orientation="Horizontal" Margin="22,0,0,8">
                        <TextBlock Text="Hotkey:  " VerticalAlignment="Center" />
                        <TextBox x:Name="SearchHotkeyBox" Width="160" IsReadOnly="True" PreviewKeyDown="OnSearchHotkeyBoxKeyDown" GotKeyboardFocus="OnSearchHotkeyBoxGotFocus" LostKeyboardFocus="OnSearchHotkeyBoxLostFocus" ToolTip="Click here, then press the key combination you want — Esc or click elsewhere to finish" />
                        <TextBlock x:Name="SearchHotkeyStatus" Margin="8,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Button x:Name="InstallAppButton" Content="Install TabDesktop" Padding="10,4" Click="OnInstallApp" ToolTip="Copies the app into your user programs folder, adds it to the Start Menu and Add/Remove Programs, and turns on run-on-startup — the exe you launched can stay where it is" />
                    </StackPanel>

                    <TextBlock Text="Browser extension" FontWeight="Bold" FontSize="14" Margin="0,4,0,4" />
                    <TextBlock x:Name="ExtensionStatusText" Margin="0,0,0,6" TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Button Content="Install / update the extension…" Padding="10,4" Click="OnOpenExtensionInstall" />
                        <Button x:Name="RebuildRestartButton" Content="Rebuild and restart TabDesktop" Padding="10,4" Margin="8,0,0,0" Click="OnRebuildRestartClick" />
                    </StackPanel>

                    <TextBlock x:Name="TabGroupsHeader" Text="Tab groups" FontWeight="Bold" FontSize="14" Margin="0,4,0,4" />
                    <ItemsControl x:Name="SettingsGroups" Margin="0,0,0,16">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Expander IsExpanded="{Binding IsExpanded, Mode=TwoWay}" Margin="0,2">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Header}" VerticalAlignment="Center" />
                                            <Button Content="{Binding SizeButtonText}" Margin="14,0,0,0" Padding="8,2" Click="OnToggleGroupSize" />
                                        </StackPanel>
                                    </Expander.Header>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource SectionMaxHeight}}">
                                        <ItemsControl ItemsSource="{Binding Titles}" Margin="26,4,0,4">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Expander>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Saved right-click state" FontWeight="Bold" FontSize="14" Margin="0,4,0,4" />
                    <Expander Header="Browse saved state (domains, programs, expanded windows)" Expanded="OnSavedStateExpanded">
                        <DockPanel Margin="0,8,0,0">
                            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                                <TextBlock Text="Search:  " VerticalAlignment="Center" />
                                <TextBox x:Name="SavedStateSearch" TextChanged="OnSavedStateSearch" />
                            </DockPanel>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource SectionMaxHeight}}">
                                <ItemsControl x:Name="SavedStateList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel Margin="0,1">
                                                <Button DockPanel.Dock="Right" Content="Remove" Padding="8,1" Margin="8,0,0,0" Click="OnRemoveSavedState" />
                                                <TextBlock Text="{Binding Category}" Width="190" Foreground="Gray" VerticalAlignment="Center" />
                                                <TextBlock Text="{Binding Value}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" ToolTip="{Binding Value}" />
                                            </DockPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </DockPanel>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </TabItem>
        <TabItem Header="Layout">
            <Viewbox Stretch="Uniform">
                <Border x:Name="DesktopBounds" Background="#FF1E1E1E">
                    <ItemsControl x:Name="LayoutItems">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding CanvasLeft}" />
                                <Setter Property="Canvas.Top" Value="{Binding CanvasTop}" />
                                <Setter Property="Panel.ZIndex" Value="{Binding ZIndex}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="{Binding Width}" Height="{Binding Height}" BorderThickness="3" BorderBrush="#FF4A9EDE" Background="#FF2A2A2A" ClipToBounds="True">
                                    <DockPanel>
                                        <TextBlock DockPanel.Dock="Top" Text="{Binding CountText}" FontSize="72" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,6" />
                                        <ItemsControl ItemsSource="{Binding Members}" Margin="14,0,14,10">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding DisplayTitle}" FontSize="48" Foreground="White" TextTrimming="CharacterEllipsis" Margin="0,2" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </Viewbox>
        </TabItem>
        <TabItem Header="List">
            <ListView x:Name="WindowList">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="ToolTip">
                            <Setter.Value>
                                <Image Source="{Binding Thumbnail}" MaxWidth="480" MaxHeight="320" Stretch="Uniform" />
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Thumbnail}" Value="{x:Null}">
                                <Setter Property="ToolTipService.IsEnabled" Value="False" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Title" Width="420">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <local:MiddleEllipsisTextBlock FullText="{Binding Title}" VerticalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Position" Width="110" DisplayMemberBinding="{Binding PositionText}" />
                        <GridViewColumn Header="Size" Width="110" DisplayMemberBinding="{Binding SizeText}" />
                        <GridViewColumn Header="PID" Width="70" DisplayMemberBinding="{Binding Pid}" />
                        <GridViewColumn Header="Status" Width="110" DisplayMemberBinding="{Binding Status}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </TabItem>
        <TabItem x:Name="ThumbnailsTab" Header="Thumbnails">
            <DockPanel Margin="10,8">
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                    <TextBlock Text="Search:  " VerticalAlignment="Center" />
                    <Button x:Name="ThumbnailRefreshButton" DockPanel.Dock="Right" Content="Refresh" Padding="10,2" Margin="12,0,0,0" Click="OnThumbnailRefresh" />
                    <TextBlock x:Name="ThumbnailStaleText" DockPanel.Dock="Right" Text="Out of date" Foreground="#CC7700" FontWeight="Bold" Margin="12,0,0,0" VerticalAlignment="Center" Visibility="Collapsed" ToolTip="Thumbnails or titles changed since this list was loaded — a new search refreshes automatically, or click Refresh" />
                    <TextBlock x:Name="ThumbnailCountText" DockPanel.Dock="Right" Margin="12,0,0,0" VerticalAlignment="Center" Foreground="Gray" />
                    <TextBox x:Name="ThumbnailSearchBox" TextChanged="OnThumbnailSearch" ToolTip="Case-insensitive contains match against every field of the row: title, URL, file hash, last-used date, and size" />
                </DockPanel>
                <ListView x:Name="ThumbnailList">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Thumbnail" Width="110">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding Image}" Height="48" Stretch="Uniform" HorizontalAlignment="Left" RenderOptions.BitmapScalingMode="HighQuality" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Title" Width="300">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <local:MiddleEllipsisTextBlock FullText="{Binding Title}" VerticalAlignment="Center" ToolTip="{Binding Title}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="URL" Width="340">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <local:MiddleEllipsisTextBlock FullText="{Binding Url}" VerticalAlignment="Center" ToolTip="{Binding Url}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Last used" Width="120" DisplayMemberBinding="{Binding LastUsedText}" />
                            <GridViewColumn Header="Size" Width="70" DisplayMemberBinding="{Binding SizeText}" />
                            <GridViewColumn Header="File hash" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <local:MiddleEllipsisTextBlock FullText="{Binding Hash}" VerticalAlignment="Center" ToolTip="{Binding Hash}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </TabItem>
        <TabItem Header="Threads">
            <DockPanel>
                <TextBlock x:Name="WorkerSummary" DockPanel.Dock="Top" Margin="10,8" FontWeight="Bold" />
                <ListView x:Name="WorkerList">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="PID" Width="80" DisplayMemberBinding="{Binding Pid}" />
                            <GridViewColumn Header="State" Width="90" DisplayMemberBinding="{Binding State}" />
                            <GridViewColumn Header="Last poll took" Width="130" DisplayMemberBinding="{Binding Took}" />
                            <GridViewColumn Header="Last poll" Width="520">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <local:MiddleEllipsisTextBlock FullText="{Binding Poll}" VerticalAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </TabItem>
        <TabItem Header="Log">
            <DockPanel>
                <TextBox DockPanel.Dock="Bottom" Height="160" IsReadOnly="True" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Text="{Binding SelectedItem.Message, ElementName=LogList, Mode=OneWay}" />
                <ListView x:Name="LogList">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Time" Width="80" DisplayMemberBinding="{Binding TimeText}" />
                            <GridViewColumn Header="Source" Width="140" DisplayMemberBinding="{Binding Source}" />
                            <GridViewColumn Header="Message" Width="720">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <local:MiddleEllipsisTextBlock FullText="{Binding FirstLine}" VerticalAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </TabItem>
    </TabControl>
</Window>
